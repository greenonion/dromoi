{
  "name": "dromoi",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot dev-server.js",
    "build": "bun build index.html --outdir dist --minify && bun run copy:assets",
    "copy:assets": "cp tetrachords.yml dist/",
    "test": "bun test"
  },
  "dependencies": {
    "vexflow": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
